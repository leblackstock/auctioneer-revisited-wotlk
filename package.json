{
  "name": "auctioneer-revisited-wotlk",
  "version": "5.9.4961-revisited.1",
  "private": true,
  "description": "Development, validation, and packaging tools for Auctioneer Revisited for WotLK 3.3.5a.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:lua": "node scripts/validate-lua.cjs",
    "package": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package.ps1",
    "verify:package": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-package.ps1",
    "validate": "npm test && npm run test:lua && npm run package && npm run verify:package"
  },
  "devDependencies": {
    "fengari": "0.1.5",
    "luaparse": "0.3.1"
  }
}
